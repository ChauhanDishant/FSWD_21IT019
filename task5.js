const books = ["RichDadPoorDad", "Ramayana", "Mahabharata", "Hello", "Pearson Harbour"]

function favbooks(books) {
    for (let i = 0; i < books.length; i++) {
        console.log(books[i]);
    }
}

favbooks(books);